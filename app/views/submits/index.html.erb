<h1>Submit#index</h1>
<p>Find me in app/views/submit/index.html.erb</p>
<div class="container">
    <div class="row">
        <div class="col s12 m4">
            <div class="card">
                <div class="card-image">
                    <img src="http://materializecss.com/images/office.jpg" />
                </div>
                <div class="card-content">
                    <div class="card-title"><%= current_user.name %></div>
                    <p>이메일 : <%= current_user.email %></p>
                    <p>올린 글 개수 : <%= @submits_count %></p>

                    <%= link_to new_submit_path do %>
                        <button class="btn">글 쓰기</button>
                    <% end %>
                </div>
            </div>
        </div>
        <div class="col s12 m8">
            <% @submits.each do |submit| %>
                <div class="card">
                    <div class="card-content">
                        <span class="card-title"><%= submit.user.name %></span>
                        <span><%= submit.created_at %></span>
                        <p><%= submit.content %></p>
                    </div>

                    <div class="card-action">
                        <a href="#">좋아요</a>
                        <a href="#">댓글 달기</a>
                    </div>
                </div>
            <% end %>
        </div>
    </div>
</div>